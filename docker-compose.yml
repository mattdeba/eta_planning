services:
  postgres:
    image: postgres:16-alpine
    container_name: my_eta_planning_postgres
    environment:
      POSTGRES_DB: eta_planning
      POSTGRES_USER: eta
      POSTGRES_PASSWORD: eta
    ports:
      - '5433:5432'
    volumes:
      - eta-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U eta -d eta_planning']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  eta-postgres-data:
